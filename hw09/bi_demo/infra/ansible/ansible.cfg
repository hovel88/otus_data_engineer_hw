[defaults]
log_path  = ansible.log
inventory = hosts.ini
host_key_checking = False
remote_user = yc-user
private_key_file = ~/.ssh/yc_empty_rsa

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False

[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no
