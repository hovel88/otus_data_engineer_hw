[project]
name = "bi-demo"
version = "0.1.0"
description = "Project to deploy Metabase as BI tool"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "ansible==6.7.0",
    "invoke==2.2.1",
]
